{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "outDir": "./dist",
    "rootDir": "./src"
  },
  // Specs live next to the code they test, so they must be excluded explicitly
  // or `dist/` ships them — and with them `vitest` and `cloudflare:test` imports
  // in a consumer's runtime graph.
  "include": ["src/**/*.ts"],
  "exclude": ["src/**/*.spec.ts"]
}
